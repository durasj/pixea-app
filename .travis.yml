language: node_js
node_js:
   - "lts/*"

cache:
  yarn: true
  directories:
    - node_modules

before_script:
   - yarn -g install firebase-tools
   - yarn -g install @angular/cli

script:
   - ng build --prod --aot
